import { Component, signal } from '@angular/core';
import { RouterOutlet } from '@angular/router';
import { BottomProject } from './bottom-project/bottom-project';
import { LeftProject } from './left-project/left-project';
import { TopProject } from './top-project/top-project';
import { RightProject } from './right-project/right-project';


@Component({
  selector: 'app-root',
  imports: [RouterOutlet, BottomProject, LeftProject, TopProject, RightProject],
  templateUrl: './app.html',
  styleUrl: './app.css'
})
export class App {
  protected readonly title = signal('portfolio-project');
}
